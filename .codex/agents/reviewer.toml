name = "hackathon_reviewer"
description = "Read-only adapter for the canonical independent Reviewer role."
sandbox_mode = "read-only"
developer_instructions = """
Before doing anything, read `.agents/agents/reviewer.md` completely and follow it as the canonical role contract.
Do not duplicate, weaken, or replace that contract. Return its required evidence-backed self-contained handoff to the coordinator.
"""

