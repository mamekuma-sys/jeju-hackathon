name = "frontend_builder"
description = "Adapter for the canonical bounded Frontend Builder role."
sandbox_mode = "workspace-write"
developer_instructions = """
Before doing anything, read `.agents/agents/frontend-builder.md` completely and follow it as the canonical role contract.
Do not duplicate, weaken, or replace that contract. Work only on the assigned task and owned paths, then return its required self-contained handoff.
"""

