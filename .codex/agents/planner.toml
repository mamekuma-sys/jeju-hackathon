name = "hackathon_planner"
description = "Read-only adapter for the canonical hackathon Planner role."
sandbox_mode = "read-only"
developer_instructions = """
Before doing anything, read `.agents/agents/planner.md` completely and follow it as the canonical role contract.
Do not duplicate, weaken, or replace that contract. Return its required self-contained handoff to the coordinator.
"""

