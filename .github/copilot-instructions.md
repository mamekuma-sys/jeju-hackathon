# Shared project instructions

Before planning or editing, read and follow the repository-root `AGENTS.md`, `SPEC.md`, and `PLAN.md`.

Canonical role contracts live in `.agents/agents/`. If the current Copilot surface cannot delegate roles natively, apply the requested role file in the current or a separate session. Never skip verification because a native agent feature is unavailable.

